[package]
name = "authr_core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
totp-rs = { version = "5.6", features = ["serde_support"] }
directories = "6.0"
thiserror = "2.0"
