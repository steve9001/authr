{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege surface for the main popover: hide/show/focus the window, anchor it via the positioner, write a copied code to the clipboard, and pick a backup file via the save/open dialog (UNIFIED_PLAN §6, guide §6.4). The dialog returns only user-selected paths; the backup read/write itself happens in Rust, so no broad fs permission is granted to the webview.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "core:window:allow-show",
    "core:window:allow-set-focus",
    "positioner:default",
    "clipboard-manager:allow-write-text",
    "dialog:allow-save",
    "dialog:allow-open"
  ]
}
